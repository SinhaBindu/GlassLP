@using System.Data
@{
    DataTable dt = new DataTable();
    if (Model != null)
    {
        dt = Model;
    }
}
<style>
    table.dataTable thead > tr > th.sorting, table.dataTable thead > tr > th.sorting_asc, table.dataTable thead > tr > th.sorting_desc, table.dataTable thead > tr > th.sorting_asc_disabled, table.dataTable thead > tr > th.sorting_desc_disabled, table.dataTable thead > tr > td.sorting, table.dataTable thead > tr > td.sorting_asc, table.dataTable thead > tr > td.sorting_desc, table.dataTable thead > tr > td.sorting_asc_disabled, table.dataTable thead > tr > td.sorting_desc_disabled {
        padding-right: 0px !important;
    }
</style>

<table class="table table-bottom-border table-lg align-middle tbl" id="tbl">
    <thead class="text-center">
        <tr>
            <th class="text-center tblth">Srno.</th>
            <th class="text-center tblth">Business Mentor Name</th>
            <th class="text-center tblth">VE Name</th>
            <th class="text-center tblth">AOP Name</th>
            <th class="text-center tblth">CLF Name</th>
            <th class="text-center tblth">Dated</th>
            <th class="text-center tblth">Action</th>
        </tr>
    </thead>
    <tbody>
        @{
            if (dt.Rows.Count > 0)
            {
                int srno = 1;
                foreach (DataRow dr in dt.Rows)
                {
                    <tr>
                        <td class="text-center">@(srno++)</td>
                        <td class="text-left">@dr["BusinessMentorName"]</td>
                        <td class="text-left">@dr["VEName"]</td>
                        <td class="text-left">@dr["AOPName"]</td>
                        <td class="text-left">@dr["CLFName"]</td>
                        <td class="text-left">@dr["Dated"]</td>
                        <td class="edit">
                            <a asp-action="Create"
                               asp-route-PId="ParticipantId_pk"
                               class="btn edit-item-btn icon-btn btn-outline-info">
                                <i class="fa fa-edit"></i>
                            </a>

                            <button class="btn edit-item-btn icon-btn btn-outline-success" asp-action="Details" asp-route-PId="ParticipantId_pk">
                                <i class="fa fa-eye"></i>
                            </button>
                        </td>
                    </tr>
                }
            }
            else
            {
                <tr>
                    <td colspan="16" class="text-center"><strong class="text-danger">Record Not Found !!</strong></td>
                </tr>
            }
        }
    </tbody>
</table>

